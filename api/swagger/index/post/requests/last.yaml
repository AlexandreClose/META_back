type: object
properties:
    name:
        type: string
        description: "Nom du jeux de donnée"
        example: "candelabre_park_status"
    columns:
        type: string
        description: "List des colonne à récupéré"
        example:
            - "status"
            - "id"
            - "place"
    groupby:
        type: string
        description: "Paramètre de jointure, noter le + pour faire un groupby sur deux colonnes"
        example: "id+place"
